<!-- DS14: Keep the System namespace while this branch uses explicit system:String style entries. -->
<DefaultWindow
    xmlns="https://spacestation14.io"
    xmlns:system="clr-namespace:System;assembly=System.Runtime"
    xmlns:ui="clr-namespace:Content.Client.Materials.UI"
    Title="{Loc 'lathe-menu-title'}"
    MinSize="550 450"
    SetSize="750 500">
    <BoxContainer
        Orientation="Horizontal"
        VerticalExpand="True"
        HorizontalExpand="True"
        SeparationOverride="5">
        <!-- Left Col-->
        <BoxContainer
            VerticalExpand="True"
            HorizontalExpand="True">
            <BoxContainer
                Orientation="Vertical"
                VerticalExpand="True"
                HorizontalExpand="True"
                SeparationOverride="5">
                <BoxContainer Orientation="Horizontal"
                    HorizontalExpand="True">
                    <LineEdit
                        Name="SearchBar"
                        PlaceHolder="{Loc 'lathe-menu-search-designs'}"
                        HorizontalExpand="True">
                    </LineEdit>
                    <!-- DS14-start: ordinary dropdown chrome and popup rows are global. -->
                    <OptionButton
                        Name="FilterOption"
                        MinWidth="100" />
                    <!-- DS14-end -->
                </BoxContainer>
                <BoxContainer Orientation="Vertical"
                    MinHeight="225"
                    VerticalExpand="True"
                    HorizontalExpand="True"
                    SizeFlagsStretchRatio="4">
                    <PanelContainer VerticalExpand="True" StyleClasses="PanelDeep">
                        <ScrollContainer VerticalExpand="True" HScrollEnabled="False">
                            <BoxContainer
                                Name="RecipeList"
                                Orientation="Vertical"
                                HorizontalExpand="True"
                                VerticalExpand="True"
                                RectClipContent="True">
                            </BoxContainer>
                        </ScrollContainer>
                    </PanelContainer>
                </BoxContainer>
                <BoxContainer Orientation="Horizontal"
                    HorizontalExpand="True">
                    <Label Margin="8 0 8 0" Text="{Loc 'lathe-menu-amount'}" />
                    <LineEdit
                        Name="AmountLineEdit"
                        PlaceHolder="0"
                        Text="1"
                        HorizontalExpand="True" />
                    <Label Name="RecipeCount" Margin="8 0 8 0" MinWidth="90" Align="Right" />
                </BoxContainer>
            </BoxContainer>
        </BoxContainer>
        <BoxContainer MinWidth="5"/>
        <!-- Right Col-->
        <BoxContainer
            Orientation="Vertical"
            VerticalExpand="True"
            HorizontalExpand="True"
            SeparationOverride="5">
            <BoxContainer Orientation="Vertical" MinHeight="225">
                <BoxContainer
                    Orientation="Horizontal"
                    Align="End"
                    HorizontalExpand="True">
                    <Button
                        Name="ServerListButton"
                        Text="{Loc 'lathe-menu-server-list'}"
                        TextAlign="Center"
                        Mode="Press">
                    </Button>
                </BoxContainer>
                <!-- DS14-start -->
                <Label Text="{Loc 'lathe-menu-queue-title'}" Margin="5 5 5 5" HorizontalAlignment="Center" StyleClasses="DeadSpaceSectionTitle" />
                <!-- DS14-end -->
                <BoxContainer
                    Name="FabricatingContainer"
                    Orientation="Horizontal"
                    HorizontalExpand="True"
                    SizeFlagsStretchRatio="2">
                    <!-- DS14-start: use the shared semantic success surface for active fabrication. -->
                    <PanelContainer
                        VerticalExpand="True"
                        HorizontalExpand="True"
                        SizeFlagsStretchRatio="3"
                        StyleClasses="positive">
                        <!-- DS14-end -->
                        <Label
                            RectClipContent="False"
                            HorizontalAlignment="Left"
                            Margin="5 0 0 0"
                            Text="{Loc 'lathe-menu-fabricating-message'}">
                        </Label>
                        <BoxContainer Name="FabricatingDisplayContainer"
                                      HorizontalAlignment="Left"
                                      Margin="100 0 0 0"/>
                        <Label
                            Name="NameLabel"
                            RectClipContent="True"
                            HorizontalAlignment="Left"
                            Margin="130 0 0 0">
                        </Label>
                        <Button
                            Name="DeleteFabricating"
                            Margin="0"
                            Text="✖"
                            SetSize="38 32"
                            HorizontalAlignment="Right"
                            ToolTip="{Loc 'lathe-menu-delete-fabricating-tooltip'}">
                            <Button.StyleClasses>
                                <system:String>Caution</system:String>
                                <system:String>OpenLeft</system:String>
                            </Button.StyleClasses>
                        </Button>
                    </PanelContainer>
                </BoxContainer>
                <ScrollContainer VerticalExpand="True" HScrollEnabled="False">
                    <BoxContainer
                        Name="QueueList"
                        Orientation="Vertical"
                        HorizontalExpand="True"
                        VerticalExpand="True"
                        RectClipContent="True">
                    </BoxContainer>
                </ScrollContainer>
            </BoxContainer>
            <BoxContainer
            VerticalExpand="True"
            HorizontalExpand="True"
            Orientation="Vertical">
            <!-- DS14-start -->
            <Label Text="{Loc 'lathe-menu-materials-title'}" Margin="5 5 5 5" HorizontalAlignment="Center" StyleClasses="DeadSpaceSectionTitle" />
            <!-- DS14-end -->
            <PanelContainer VerticalExpand="True" StyleClasses="PanelDeep">
                <BoxContainer
                    Orientation="Vertical"
                    VerticalExpand="True"
                    HorizontalExpand="True">
                    <ui:MaterialStorageControl Name="MaterialsList" SizeFlagsStretchRatio="8"/>
                </BoxContainer>
            </PanelContainer>
        </BoxContainer>
        </BoxContainer>

    </BoxContainer>

</DefaultWindow>
