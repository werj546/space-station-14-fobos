<cartridges:NanoTaskUiFragment xmlns:cartridges="clr-namespace:Content.Client.CartridgeLoader.Cartridges"
                               xmlns="https://spacestation14.io" Margin="1 0 2 0">

    <PanelContainer StyleClasses="DeadSpaceSurfaceDark"></PanelContainer> <!-- DS14 -->
    <BoxContainer Orientation="Vertical" HorizontalExpand="True" VerticalExpand="True">
        <ScrollContainer HorizontalExpand="True" VerticalExpand="True">
            <BoxContainer HorizontalExpand="True" VerticalExpand="True" Orientation="Vertical" Margin="8" SeparationOverride="8">
                <!-- Heading for High Priority Items -->
                <BoxContainer Orientation="Horizontal">
                    <!-- DS14-start -->
                    <PanelContainer SetWidth="7" Margin="0 0 8 0" StyleClasses="negative" />
                    <!-- DS14-end -->
                    <Label Name="HighPriority" StyleClasses="DeadSpaceListHeader"/> <!-- DS14 -->
                </BoxContainer>
                <!-- Location for High Priority Items -->
                <GridContainer Name="HighContainer"
                               HorizontalExpand="True"
                               Access="Public"
                               Columns="2" />

                <!-- Heading for Medium Priority Items -->
                <BoxContainer Orientation="Horizontal">
                    <!-- DS14-start -->
                    <PanelContainer SetWidth="7" Margin="0 0 8 0" StyleClasses="highlight" />
                    <!-- DS14-end -->
                    <Label Name="MediumPriority" StyleClasses="DeadSpaceListHeader"/> <!-- DS14 -->
                </BoxContainer>
                <!-- Location for Medium Priority Items -->
                <GridContainer Name="MediumContainer"
                               HorizontalExpand="True"
                               Access="Public"
                               Columns="2" />

                <!-- Location for Low Priority Items -->
                <BoxContainer Orientation="Horizontal">
                    <!-- DS14-start -->
                    <PanelContainer SetWidth="7" Margin="0 0 8 0" StyleClasses="positive" />
                    <!-- DS14-end -->
                    <Label Name="LowPriority" StyleClasses="DeadSpaceListHeader"/> <!-- DS14 -->
                </BoxContainer>
                <!-- Location for Low Priority Items -->
                <GridContainer Name="LowContainer"
                               HorizontalExpand="True"
                               Access="Public"
                               Columns="2" />

                <Button Name="NewTaskButton" Text="{Loc 'nano-task-ui-new-task'}" HorizontalAlignment="Right" StyleClasses="DeadSpaceAction"/> <!-- DS14 -->
            </BoxContainer>
        </ScrollContainer>
    </BoxContainer>
</cartridges:NanoTaskUiFragment>
