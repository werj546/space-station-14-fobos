<controls:FancyWindow  xmlns="https://spacestation14.io"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
    Name="APCMenu"
    Title="{Loc 'apc-menu-title'}"
    Resizable="False">

    <BoxContainer Orientation="Vertical">
        <BoxContainer Margin="0 5 10 10" Orientation="Vertical">
            <BoxContainer Margin="0" Orientation="Horizontal">
                <!-- Sprite View -->
                <!-- DS14-start -->
                <PanelContainer Margin="0" StyleClasses="DeadSpaceInset" VerticalAlignment="Center">
                    <SpriteView Name="EntityView" Scale="2 2" SetSize="64 64" OverrideDirection="South" />
                </PanelContainer>
                <!-- DS14-end -->
                <!-- Data -->
                <BoxContainer Orientation="Vertical" HorizontalExpand="True">
                    <GridContainer Margin="10 0 0 0" Columns="2">
                        <!-- Power On/Off -->
                        <!-- DS14-start -->
                        <Label Text="{Loc 'apc-menu-breaker-label'}" HorizontalExpand="True"
                                StyleClasses="DeadSpaceSectionTitle" MinWidth="120"/>
                        <controls:SwitchButton Name="BreakerButton" MinWidth="150"/>
                        <!--Charging Status-->
                        <Label Text="{Loc 'apc-menu-external-label'}" StyleClasses="DeadSpaceSectionTitle" MinWidth="120" />
                        <Label Name="ExternalPowerStateLabel" Text="{Loc 'apc-menu-power-state-good'}" />
                        <!--Battery Power-->
                        <Label Text="{Loc 'apc-menu-power-label'}" StyleClasses="DeadSpaceSectionTitle" MinWidth="120" />
                        <!-- DS14-end -->
                        <Label Name="PowerLabel"/>
                    </GridContainer>
                </BoxContainer>
            </BoxContainer>
            <!-- Charge Progress Bar-->
            <ProgressBar Name="ChargeBar"
                        HorizontalExpand="True"
                        MinValue="0"
                        MaxValue="1"
                        MinHeight = "25"
                        Page="0"
                        Margin="10 10 0 0"
                        Value="0.5">
                <Label Name="ChargePercentage" Margin="4 0" Text="0 %" />
            </ProgressBar>
        </BoxContainer>

        <!-- Footer -->
        <BoxContainer Orientation="Vertical">
            <PanelContainer StyleClasses="LowDivider" />
            <BoxContainer Orientation="Horizontal" Margin="10 2 5 0" VerticalAlignment="Bottom">
                <!-- DS14-start -->
                <Label Text="{Loc 'apc-menu-flavor-left'}" StyleClasses="DeadSpaceSubtitle" />
                <Label Text="{Loc 'apc-menu-flavor-right'}" StyleClasses="DeadSpaceSubtitle"
                        HorizontalAlignment="Right" HorizontalExpand="True"  Margin="0 0 5 0" />
                <!-- DS14-end -->
                <TextureRect StyleClasses="NTLogoDark" Stretch="KeepAspectCentered"
                        VerticalAlignment="Center" HorizontalAlignment="Right" SetSize="19 19"/>
            </BoxContainer>
        </BoxContainer>
    </BoxContainer>
</controls:FancyWindow>
