<Control xmlns="https://spacestation14.io">
    <BoxContainer Margin="8,0,8,8" Orientation="Vertical">
        <BoxContainer Orientation="Horizontal">
            <!-- DS14-start -->
            <Label Name="StoreItemName" HorizontalExpand="True" StyleClasses="DeadSpaceSectionTitle" />
            <!-- DS14-end -->
        </BoxContainer>
        <PanelContainer StyleClasses="HighDivider" Margin="0,1,0,2"/>
        <BoxContainer HorizontalExpand="True" Orientation="Horizontal">
            <BoxContainer Orientation="Vertical" VerticalAlignment="Center" Margin="0,0,4,0">
                <TextureRect
                    Name="StoreItemTexture"
                    Margin="0,0,4,0"
                    MinSize="48 48"
                    Stretch="KeepAspectCentered" />
                <EntityPrototypeView
                    Name="StoreItemPrototype"
                    Margin="0,0,4,0"
                    MinSize="48 48"
                    Stretch="Fill"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center" />
                <!-- DS14-start -->
                <Button
                    Name="StoreItemBuyButton"
                    MinWidth="48"
                    MaxHeight="48"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    Margin="0,4,0,0"
                    Access="Public"
                    StyleClasses="DeadSpaceAction" />
                <!-- DS14-end -->
                <Label Name="DiscountSubText"
                       HorizontalAlignment="Center"/>
            </BoxContainer>
            <RichTextLabel Name="StoreItemDescription" VerticalAlignment="Top"/>
        </BoxContainer>
    </BoxContainer>
</Control>
