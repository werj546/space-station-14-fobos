<ContainerButton xmlns="https://spacestation14.io"
                 HorizontalExpand="True">
    <!-- DS14-start -->
    <BoxContainer Orientation="Horizontal"
                  HorizontalExpand="True"
                  SeparationOverride="0"
                  Name="InternalHBox">
        <PanelContainer StyleClasses="DeadSpaceCharacterIcon"
                        Margin="0 4 6 4">
            <SpriteView Scale="2 2"
                        OverrideDirection="South"
                        Name="View"
                        SetSize="64 64"/>
        </PanelContainer>
        <Label Name="DescriptionLabel"
               ClipText="True"
               VerticalAlignment="Center"
               HorizontalExpand="True"/>
        <Button Name="DeleteButton"
                Text="{Loc 'character-setup-gui-character-picker-button-delete-button'}"/>
        <Button Name="ConfirmDeleteButton"
                Text="{Loc 'character-setup-gui-character-picker-button-confirm-delete-button'}"
                Visible="False"
                StyleClasses="negative"/>
    </BoxContainer>
    <!-- DS14-end -->
</ContainerButton>
