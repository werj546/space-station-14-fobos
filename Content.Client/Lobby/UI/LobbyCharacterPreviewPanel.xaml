<Control
    xmlns="https://spacestation14.io"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls">
    <!-- DS14-start: KSO-styled lobby character preview -->
    <PanelContainer StyleClasses="DeadSpaceInset"
                    HorizontalExpand="True">
        <BoxContainer Name="VBox"
                      Orientation="Vertical"
                      HorizontalExpand="True"
                      SeparationOverride="4">
            <controls:NanoHeading Name="Header" Text="{Loc 'lobby-character-preview-panel-header'}" Visible="False">

            </controls:NanoHeading>
            <BoxContainer Name="Loaded"
                          Orientation="Vertical"
                          Visible="False"
                          HorizontalExpand="True">
                <Label Name="Summary" HorizontalAlignment="Center" Margin="3 3"/>
                <BoxContainer Name="ViewBox" Orientation="Horizontal" HorizontalAlignment="Center">

                </BoxContainer>
                <controls:VSpacer/>
                <Button Name="CharacterSetup" Text="{Loc 'lobby-character-preview-panel-character-setup-button'}"
                        HorizontalAlignment="Center"
                        StyleClasses="DeadSpaceTopAction"
                        Margin="0 5 0 0" Visible="False"/>
            </BoxContainer>
            <Label Name="Unloaded"
                   Text="{Loc 'lobby-character-preview-panel-unloaded-preferences-label'}"
                   StyleClasses="DeadSpaceSubtitle"
                   HorizontalAlignment="Center"/>
        </BoxContainer>
    </PanelContainer>
    <!-- DS14-end -->
</Control>
